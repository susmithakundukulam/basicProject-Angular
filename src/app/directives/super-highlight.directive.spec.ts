import { SuperHighlightDirective } from './super-highlight.directive';

describe('SuperHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new SuperHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
